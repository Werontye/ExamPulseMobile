// Auth screen placeholder — disabled from navigation for now
import React from 'react';
import { View, Text } from 'react-native';
export function AuthScreen() {
  return <View style={{ flex: 1, backgroundColor: '#0a0a0f', alignItems: 'center', justifyContent: 'center' }}>
    <Text style={{ color: 'white' }}>Auth — Coming Soon</Text>
  </View>;
}
